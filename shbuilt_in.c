#include "main.h"

/**
 * built_in - evaluates built in commands
 * @args: command line arguements
 * Return: -1 if no built in function is found else 1
 */

int built_in(char **args)
{
	if (args == NULL)
		return (1);
	return (-1);
}
